
const styles = (theme) => ({
    chartRoot: {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        margin: theme.spacing(1),
    },
});

export default styles;
