const styles = (theme) => ({
    root: {
        width: '100%',
        marginTop: theme.spacing(3),
        padding: theme.spacing(0, 2),
        overflowX: 'auto',
    },
});

export default styles;
